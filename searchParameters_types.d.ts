type searchParameters = {
    startIndex: string
    count: string
    q: string
    categoryIds: string,
    supportingCost: string,
    ownerId: string,
    supporterId: string,
    sortBy: string
}